rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    match /dome_lite.catalogs/{catalog_key}/{path=**} {
      allow read, write: if request.auth.uid == catalog_key && request.auth.token.token_scope == 'catalog';
    }

  }
}
